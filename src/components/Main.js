import React from 'react';

import WidgetList from './WidgetList';
import AddButton from './AddButton';

const Main = () => {
  return (
    <div>
      <AddButton />
      <WidgetList />
    </div>
  );
};

export default Main;
